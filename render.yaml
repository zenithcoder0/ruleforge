services:
  - type: web
    name: ruleforge-backend
    runtime: dotnet
    rootDir: backend
    buildCommand: dotnet build
    startCommand: dotnet run --no-launch-profile --urls=http://0.0.0.0:$PORT
